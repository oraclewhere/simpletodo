---
id: task-0001
type: full
title: 单实例检测与重复启动提示
status: todo
created: 2026-09-18
refs_decision: ["002"]
deliverables: []
context_budget:
  files:
    - main.py
    - single_instance.py（新增）
    - README.md
  est_tokens: 30k
  handoff_at: 70%
---

## 目标

同一台 Windows 机器上第二次启动 TodoSimple 时，不会出现第二个可写数据文件的进程；
用户能立即得知已有实例在运行，并被带到那个实例的界面上。

## 验收标准

> 由人填写或人工确认后生效。AI 不得自行拟定或修改本节。
> 结束时逐条回应：达标 / 未达标（未达标必须显式列出，不得静默略过）。

- [ ] <可判定的条件>
- [ ] <可判定的条件>

## 上下文预算

> 回答一个问题：**为什么这个任务能在一个 session 的注意力周期内闭环？**

- 预计涉及文件：`main.py`（启动早期插入互斥检查）、新增 `single_instance.py`、
  `README.md`（补一句重复启动行为说明）
- 预计规模：新增约 60–100 行；`main.py` 改动约 10 行
- 预计在哪一步接近上限：IPC 激活部分（把已运行实例的遮罩呼出到前台）需要
  QLocalServer 双端握手，若 stale socket 处理反复调试会消耗较多轮次
- 若超预算，拆分为：① 仅互斥检测 + 提示框后退出（R2）；
  ② 再单独做"激活已有实例并呼出遮罩"（R3 的 IPC 部分）

## 交接说明

> 会话结束、或上下文达到 `handoff_at` 时填写。这是写给"下一个 session 这个新员工"看的，
> 不是写给自己的备忘录——不要写"如上所述"这类依赖本次对话才成立的话。

- **当前进度**：未开始。等人工填写验收标准与 002 决策转 active。
- **已完成**：无
- **未完成 / 下一步（可直接执行的具体动作）**：
  1. 等 `doc/决策约束/002-单实例重复启动行为.md` 的 `status` 由人改为 `active`
  2. 按 002 的结论实现：机制用 Windows 命名互斥体（`ctypes` 调 `CreateMutexW`，
     仅 Windows 生效），提醒形态为弹框提示 + 激活已有实例并呼出遮罩
  3. 在 `main.py` 的 `QApplication` 创建之后、`TodoStore()` 实例化之前插入互斥检查
     —— **顺序很重要**：必须在任何实例读写 `todos.json` 之前拦截，否则丢数据已经发生
- **踩过的坑（试过但走不通的路，避免下一个人重走）**：
  - 不要用「启动后检测文件被占用」这类间接办法判断单实例，`storage.py` 的
    `os.replace` 写入不持有长期锁，检测不到
  - 若选 Qt 方案（QLocalServer），进程被强杀后会残留 socket 文件，
    下次启动 `listen()` 会失败，必须显式清理 stale socket。用户已选择
    「仅 Windows 生效」，用命名互斥体可完全回避这个问题（内核对象随进程销毁自动释放）
- **相关文件与位置**：
  - `storage.py:26` `load()` 只在 `__init__` 调用一次；`storage.py:33-41` `save()`
    整份覆盖写 —— 这是本任务要防的数据丢失根因
  - `main.py:95-97` 三个全局对象（`Config`/`TodoStore`/`AutoStart`）的创建位置
  - `doc/决策约束/002-单实例重复启动行为.md` 记录机制 M1–M4 与提醒形态 R1–R5 的选项对比

## 决策记录

> 执行过程中如果遇到需要人拍板的问题，记在这里，不要自行决定。
> 属于核心管理类的问题（架构、需求边界、排期）一律不在本文件内决定。

- 用户已于 2026-09-18 选定：机制「仅 Windows 生效即可」（对应 002 的 M3 命名互斥体）；
  提醒形态「弹框提示 + 激活已有实例并呼出遮罩」（对应 002 的 R3）。
- 待人工确认：002 的 `status` 需由人改为 `active` 后方可开工。