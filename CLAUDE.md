# 项目约定（AI 开发流程规范）

> 本项目启用 `ai-dev-flow` 规范。完整流程用 `/ai-dev-flow <子命令>` 调用。
> 本文件是常驻上下文的不变量，写在这里的每条规则都必须短、硬、可判定。

## 目录约定

```
code/          交付链内的代码。受硬规则约束。
scripts/       交付链外。豁免，不受约束。
doc/
  决策约束/     人维护，AI 只读
  交付件/       AI 生成，人审阅
  任务记录/     commit 的关联锚点，兼对话记录
  error/        复盘产出的违规报告，供迭代方法论
```

## 硬规则

1. 向 `code/` 的每次提交，commit message 必须含 `task-NNNN`，且该任务记录存在。
2. 合并到发布分支，commit message 必须引用 `doc/决策约束/` 或 `doc/交付件/` 下的文件。
3. `doc/决策约束/` 下的文件**不得修改**。有异议写进任务记录的「决策记录」节。
4. `code/` 之外的文件（`scripts/` 等）豁免，不需要关联任务。

> 豁免由**目录路径**决定，不由判断决定。不要用"这算不算脚本"来解释自己的任务。

## 分类先行

动手前先判定，**并说出判定结果**：

- **核心管理类**（需求对接/需求分解/架构与技术栈/原型决策/上下文预算/最终验收）
  → 输出**选项对比 + 影响范围 + 待决清单**，不是结论。产出落在 `doc/决策约束/` 草稿，等人批准。
- **工程执行类**（UI/API文档/单测/编码/联调/评审/修复/部署）
  → 直接执行，产出交付件 + 更新任务记录。

## 不可妥协的前提

**验收标准必须在执行前存在，由人写或人批准。** 不得自行拟定、不得修改、不得用
"功能正常""已测试通过"这类不可判定的措辞。未达标与无法判定的验收项必须显式列出。

## 上下文即资源

任务记录里的 `context_budget` 声明的不是时间，是**注意力预算**。
到达 `handoff_at` 之前必须完成交接——因为 session 这个"员工"离职率高、在职周期短，
所以文档先行是默认工作方式，不是离职前的临时整理。

## 现状

<!-- 由 hooks 或 /ai-dev-flow 状态 自动维护 -->
- 生效决策约束：见 `doc/决策约束/索引.md`
- 未结任务：见 `doc/任务记录/索引.md`